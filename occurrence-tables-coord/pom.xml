<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.gbif.occurrence</groupId>
		<artifactId>occurrence-motherpom</artifactId>
		<version>0.21</version>
	</parent>

	<artifactId>occurrence-tables-coord</artifactId>
	<packaging>jar</packaging>

	<name>Occurrence Table Generator Workflow/Coordinator</name>
	<description>Oozie Workflow to generate the HDFS occurrence tables</description>

	<properties>
		<!-- This timestamp format is used to set the start time in the coordinator.xml
			file -->
		<maven.build.timestamp.format>yyyy-MM-dd'T'HH:mm'Z'</maven.build.timestamp.format>
		<timestamp>${maven.build.timestamp}</timestamp>
	</properties>

	<build>
		<resources>
			<resource>
				<directory>src/main/resources</directory>
				<filtering>true</filtering>
				<includes>
					<include>conf/hive-default.xml</include>
					<include>workflow.xml</include>
					<include>hbase-site.xml</include>
					<include>coordinator.xml</include>
					<include>job.properties</include>
				</includes>
			</resource>
			<resource>
				<directory>src/main/resources</directory>
				<filtering>false</filtering>
			</resource>
		</resources>
		<plugins>
			<plugin>
				<artifactId>maven-resources-plugin</artifactId>
				<executions>
					<execution>
						<id>process-resources</id>
						<phase>process-resources</phase>
						<goals>
							<goal>resources</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<artifactId>maven-assembly-plugin</artifactId>
				<configuration>
					<appendAssemblyId>false</appendAssemblyId>
					<finalName>oozie-workflow</finalName>
					<descriptors>
						<descriptor>src/main/assembly/assembly.xml</descriptor>
					</descriptors>
				</configuration>
				<executions>
					<execution>
						<id>make-assembly</id>
						<phase>package</phase>
						<goals>
							<goal>single</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
	<repositories>
		<repository>
			<id>gbif-all</id>
			<url>http://repository.gbif.org/content/groups/gbif</url>
		</repository>
	</repositories>

	<dependencies>
		<dependency>
			<groupId>com.google.guava</groupId>
			<artifactId>guava</artifactId>
		</dependency>
		<dependency>
			<groupId>org.gbif</groupId>
			<artifactId>gbif-api</artifactId>
		</dependency>
		<dependency>
			<groupId>org.gbif</groupId>
			<artifactId>dwc-api</artifactId>
		</dependency>
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>slf4j-api</artifactId>
		</dependency>
		<dependency>
			<groupId>org.gbif.occurrence</groupId>
			<artifactId>occurrence-hive</artifactId>
			<exclusions>
				<exclusion>
					<groupId>org.gbif.occurrence</groupId>
					<artifactId>occurrence-processor</artifactId>
				</exclusion>
			</exclusions>
		</dependency>
		<dependency>
			<groupId>org.gbif.occurrence</groupId>
			<artifactId>occurrence-persistence</artifactId>
		</dependency>
		<dependency>
      <groupId>org.gbif.occurrence</groupId>
      <artifactId>occurrence-common</artifactId>
    </dependency>
    <dependency>
      <groupId>org.apache.hive</groupId>
      <artifactId>hive-hbase-handler</artifactId>
      <scope>runtime</scope>
    </dependency>
		<dependency>
			<groupId>org.apache.hive</groupId>
			<artifactId>hive-exec</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.apache.hive</groupId>
			<artifactId>hive-jdbc</artifactId>
			<version>${hive.version}</version>
			<scope>runtime</scope>
		</dependency>
	</dependencies>

	<profiles>
		<profile>
			<id>dev</id>
			<properties>
				<occurrence.hdfs.build.frequency>${coord:minutes(120)}</occurrence.hdfs.build.frequency>
				<hive.server2>c1n1.gbif.org</hive.server2>
				<hive.db>dev</hive.db>
				<hive.user>jenkins</hive.user>
				<hbase.table>dev_occurrence</hbase.table>
				<synctables>false</synctables>
				<create_tables_dynamically>true</create_tables_dynamically>
			</properties>
		</profile>

		<profile>
			<id>uat</id>
			<properties>
				<occurrence.hdfs.build.frequency>${coord:days(1)}</occurrence.hdfs.build.frequency>
				<hive.server2>c1n8.gbif.org</hive.server2>
				<hive.db>uat</hive.db>
				<hive.user>uat</hive.user>
				<hbase.table>uat_occurrence</hbase.table>
				<synctables>false</synctables>
				<create_tables_dynamically>false</create_tables_dynamically>
			</properties>
		</profile>

		<profile>
			<id>prod_b</id>
			<properties>
				<occurrence.hdfs.build.frequency>${coord:days(1)}</occurrence.hdfs.build.frequency>
				<hive.server2>c1n8.gbif.org</hive.server2>
				<hive.db>prod_b</hive.db>
				<hive.user>prod</hive.user>
				<hbase.table>prod_b_occurrence</hbase.table>
				<synctables>false</synctables>
				<create_tables_dynamically>false</create_tables_dynamically>
			</properties>
		</profile>
	</profiles>
</project>
